name: Publish  

on:
    push:
        branches:
            - 'build/firebase'

jobs:
    deploy-android:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout project
              uses: actions/checkout@v4

            - name: Setup Node
              uses: actions/setup-node@v4
              with:
                node-version: latest
                cache: npm
                cache-dependency-path: "./frontend/package.json"

            - name: Install dependencies
              working-directory: ./frontend
              run: npm ci

            - name: Decode Keystore
              working-directory: ./frontend
              env:
                KEYSTORE_ENCODED: ${{ secrets.KEYSTORE_ENCODED }}
              run: 
                echo $KEYSTORE_ENCODED | base64 --decode > ${{ secrets.KEYSTORE_FILE_NAME }}

            - name: Prebuild app
              working-directory: ./frontend
              env:
                KEYSTORE_FILE_NAME: ${{ secrets.KEYSTORE_FILE_NAME }}
                KEYSTORE_PASSWORD: ${{ secrets.KEYSTORE_PASSWORD }}
                KEYSTORE_KEY_ALIAS: ${{ secrets.KEYSTORE_KEY_ALIAS }}
                KEYSTORE_KEY_PASSWORD: ${{ secrets.KEYSTORE_KEY_PASSWORD }}
              run: npx expo prebuild --platform android

            - name: Move keystore into app folder
              working-directory: ./frontend
              run: mv ${{ secrets.KEYSTORE_FILE_NAME }} android/app/${{ secrets.KEYSTORE_FILE_NAME }}

            - name: Build Android
              working-directory: ./frontend/android
              env:
                SENTRY_AUTH_TOKEN: ${{ secrets.SENTRY_AUTH_TOKEN }}
              run: ./gradlew app:assembleRelease ## TODO when building for play store assemble aab

            ## TODO 
            ## 1. Setup keys for firebase and play store
            ## 2. Deploy the app

    deploy-ios:
      runs-on: macos-latest

      steps:
            - name: Checkout project
              uses: actions/checkout@v4

            - name: Setup Node
              uses: actions/setup-node@v4
              with:
                node-version: latest
                cache: npm
                cache-dependency-path: "./frontend/package.json"

            - name: Install dependencies
              working-directory: ./frontend
              run: npm ci

            - name: Prebuild app
              working-directory: ./frontend
              run: npx expo prebuild --platform ios

            ## TODO 
            ## 1. Setup certificate, provisioning profile and ExportOptions.plist
            ## 2. Build archive
            ## 3. Export IPA
            ## 4. Upload application